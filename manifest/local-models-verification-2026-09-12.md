# local models verification

Status: verified on the live machine 2026-09-12.

```text
ollama.service active and running (MainPID 780)
ollama 0.33.3
ollama-vulkan 0.33.3 installed
qwen3:8b present (5.2 GB)
no CUDA toolkit installed
```

The service is up and a model is installed. An inference request was not run in
this snapshot, so end-to-end GPU generation was not independently confirmed —
only that the runtime is present, active, and has a model loaded.

---

machines only. not for humans.
