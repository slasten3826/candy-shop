# full machine audit

Status: measured read-only on 2026-09-12 20:48-20:50 +04. Nothing was changed.

Session uptime at measurement: 3 days 2 hours. Last boot 2026-09-09 17:51.

## Identity

```text
hostname: candy-shop
machine id: f7876ea4b44c4fd1af2f30d967d6ee34
OS: Manjaro Linux 26.1.2
kernel: 7.1.13-2-MANJARO (built 2026-09-04)
architecture: x86-64
timezone: Europe/Samara (+04)
RTC: UTC, NTP active and synchronized
installed packages: 1363
```

## Hardware

```text
board: ASRock B450 Gaming K4
firmware: P8.03, dated 2023-03-01 (not updated since)
CPU: AMD Ryzen 5 5600X, 6 cores / 12 threads, boost up to 4654 MHz, microcode 0xa201211
L1d 192 KiB, L1i 192 KiB, L2 3 MiB, L3 32 MiB, single NUMA node
AMD-V virtualization
RAM: 31 GiB usable
```

Idle temperature at measurement:

```text
CPU Tctl: 50.5 C
NVMe:     35.9 C
GPU:      37 C, 14.4 W draw
```

## GPU

```text
device: NVIDIA GeForce GTX 1080 (GP104), PCI 0a:00.0, id 10de:1b80
VRAM: 8192 MiB, 706 MiB in use at measurement
compute capability: 6.1 (sm_61, Pascal)
VBIOS: 86.04.3B.40.51
driver: proprietary 580.178.04
loaded modules: nvidia, nvidia_uvm, nvidia_modeset, nvidia_drm
no compute processes running at measurement
```

The GPU audio function is present at `0a:00.1` (`10de:10f0`).

## Storage

```text
nvme0n1  476.9 GiB  system SSD (Silicon Motion SM2263EN/XT, DRAM-less)
  p1       300 MiB  vfat   EFI, mounted /boot/efi
  p2     476.6 GiB  btrfs  subvolumes /@ /@home /@cache /@log
  
sda      465.8 GiB  HGST 5400 RPM HDD
  sda1   465.8 GiB  ext4   label candy-hdd, UUID b6c2de9f-...-07380e33c66b
                          mounted /home/slasten/hdd

sdb      476.9 GiB  Samsung SSD (external USB, ASM1051 bridge)
  sdb1   476.9 GiB  ntfs   label VAULT_512, UUID 76D8C71C1B6196A8
                          mounted /run/media/slasten/VAULT_512
```

All filesystems were mounted and writable. Btrfs mounts use `subvol=` with
`compress=zstd:1`. Capacity and usage figures are in
`crystall/storage.md`, section "Capacity and usage".

## Swap

```text
type:         file, /swap/swapfile
size:         17179865088 bytes (16 GiB)
used:         4090966016 bytes (3.8 GiB) at measurement
priority:     10
filesystem:   Btrfs, subvolume /swap, has the C (no-COW) attribute set
zswap:        enabled
swappiness:   60
hibernation:  not configured
```

`/swap/swapfile` itself could not be read for attributes without root
(`Operation not permitted`); `/swap` was confirmed to carry `C`.

The 3.8 GiB of swap in use while 20 GiB of 31 GiB RAM was also in use
indicates real memory pressure at measurement time.

## Display and session

```text
session: X11
desktop: KDE Plasma 6.7.4 / kwin 6.7.4
display manager: SDDM
output: HDMI-0, 1920x1080 at 60 Hz, 477x268 mm, primary
DVI-D-0 and DP-0..DP-5 disconnected
```

## Local model stack

```text
ollama: 0.33.3-1, service active and running (MainPID 780)
ollama-vulkan: 0.33.3-1
models pulled: qwen3:8b (5.2 GB, pulled ~2026-08-31)
```

No CUDA toolkit is installed:

```text
nvcc: not found
/opt/cuda: absent
```

NVIDIA packages present:

```text
nvidia-580xx-utils 580.178.04-1
lib32-nvidia-580xx-utils 580.178.04-1
linux71-nvidia-580xx 580.178.04-12
nvidia-580xx-settings 580.178.04-1
mhwd-nvidia-580xx 580.178.04-1
mhwd-nvidia 610.57.04-1 (branch marker, not the active driver)
mhwd-nvidia-390xx, mhwd-nvidia-470xx (legacy profiles, not installed as driver)
linux-firmware-nvidia 20260810-2
nvidia-driver-assistant 0.23.48.01-7
```

No PyTorch, bitsandbytes, or Python ML environment was observed. Inference is
currently through `ollama` only, and it runs via the Vulkan backend
(`ollama-vulkan` is installed).

## Gaming stack

```text
steam 1.0.0.87-2, running at measurement (steamwebhelper active)
steam-devices 1.0.0.87-2
portproton 1.7.5-1 (AUR)
vulkan-icd-loader 1.4.357.0-1, lib32-vulkan-icd-loader 1.4.357.0-1
vulkan-tools 1.4.357.0-1
no wine, lutris, heroic, gamemode, or mangohud as system packages
```

## Xbox wireless adapter

```text
adapter present: Microsoft Xbox Wireless Adapter, USB 045e:02fe, bus 001 device 004
dkms: xone/0.5.8 built for 7.1.13-2-MANJARO, installed
loaded modules: xone_dongle, xone_gip, xone_gip_gamepad
                (xone_gip_headset not loaded, no headset attached)
```

Driver binding was not re-read at measurement time (the adapter is on the
`xone-dongle` path per loaded modules).

## Network

```text
wired ethernet   up, NetworkManager-managed (Realtek RTL8111/8168/8211/8411)
VPN tunnel       up, tun type, active
loopback         up
```

A userspace VPN is up as an active TUN interface. It is a live part of the
network path, not a leftover. Interface names and addresses are omitted from
this public snapshot.

## Services

Running at measurement:

```text
cronie, cups, dbus-broker, ModemManager, NetworkManager, ollama,
polkit, power-profiles-daemon, sddm, sshd, systemd-hostnamed,
systemd-journald, systemd-logind, systemd-timesyncd, systemd-udevd,
systemd-userdbd, udisks2, upower
```

No failed units.

```text
sshd:     active, enabled
cups:     active
firewalld: inactive
ufw:       inactive
no firewall is active
```

Listening sockets include KDE Connect (`kdeconnectd`, mDNS 5353) and Steam.

## Power

```text
power profile: performance
CPU governor: performance
frequency boost: enabled
```

## Desktop services and extras

```text
audio: PipeWire 1.6.8 + WirePlumber 0.5.17, PulseAudio compatibility layer
       (server reports "PulseAudio (on PipeWire 1.6.8)")
flatpak: installed, Flathub configured, runtime-only (Mesa, NVIDIA GL,
         KDE Platform, VAAPI, codecs) — no applications installed
timeshift: 25.12.4-1 installed, /etc/timeshift configured, autosnap config present
bluetooth: present (package level)
```

## Boot

```text
bootloader: GRUB, EFI
EFI boot entry: Manjaro (Boot0000) and UEFI OS (Boot000A, currently selected)
BootOrder: 000A, 0000, 0004, 0009
boot timeout: 1 second
```

## Journal errors this boot

Non-fatal only:

```text
kernel: virt/tdx: TDX not supported by the host platform   (expected)
steam: pressure-vessel-wrap internal assertion error on 2026-09-11
kioworker: TIFF missing ImageLength on 2026-09-12
a sudo auth failure on 2026-09-11 13:03 (password not entered)
```

No hardware or driver failure is present in the boot journal.

## Drift From Predecessor

The predecessor tree (`~/codex/candy-shop`) was measured 2026-08-21 through
2026-09-03. Facts that have changed since:

```text
kernel        updated to 7.1.13-2
VAULT_512     now mounted at /run/media/slasten/VAULT_512
ollama        "not observed installed"  ->  0.33.3 active, qwen3:8b pulled
VPN           now present as an active tunnel
swap          policy said "swap: none" at 08-21, then 16 GiB added at 08-22;
              confirmed live at 16 GiB on 09-12
```

Facts still matching the predecessor: board, CPU, RAM size, GPU and driver
branch, HDD filesystem and mount options, swap path/size/zswap/swappiness,
X11 + Plasma, HDMI 1920x1080, Steam and PortProton installed, xone installed.

The predecessor's `manifest/initial-system-audit-2026-08-21.md` states
`swap: none`, which was already false when written relative to the 08-22 swap
work. This snapshot supersedes it.

---

machines only. not for humans.
